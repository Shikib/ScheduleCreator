<%= form_for(@lecture_section) do |f| %>
  <% if @lecture_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lecture_section.errors.count, "error") %> prohibited this lecture_section from being saved:</h2>

      <ul>
      <% @lecture_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
